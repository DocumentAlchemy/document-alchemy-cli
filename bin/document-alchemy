#!/usr/bin/env node
var path = require('path');
var fs   = require('fs');
var dir  = path.join(path.dirname(fs.realpathSync(__filename)), '..', 'lib');
require(path.join(dir,'document-alchemy-cli')).main()
